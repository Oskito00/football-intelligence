services:
  frontend:
    build: 
      context: ./frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://165.227.225.47:5000
    depends_on:
      - api

  api:
    build: 
      context: .
      dockerfile: chatbot/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=localhost
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}

  scheduler:
    build:
      context: .
      dockerfile: scheduler/Dockerfile
    environment:
      - DB_HOST=localhost
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - API_FOOTBALL_KEY=${API_FOOTBALL_KEY}

# Remove these:
#  db:
#    image: postgres:15
#    ports:
#      - "5433:5432"
#    environment:
#      - POSTGRES_DB=${DB_NAME}
#      - POSTGRES_USER=${DB_USER}
#      - POSTGRES_PASSWORD=${DB_PASSWORD}
#    volumes:
#      - postgres_data:/var/lib/postgresql/data

# Remove this too since we don't need it anymore:
# volumes:
#   postgres_data:

build:
  docker:
    web: frontend/Dockerfile
    api: chatbot/Dockerfile
    scheduler: scheduler/Dockerfile