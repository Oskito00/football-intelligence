# Football Intelligence Dashboard

This is a lean Svelte dashboard shell for read-only Football Intelligence
status surfaces.

## Scripts

```bash
npm start
npm run build
```

Set `VITE_API_URL` when the API is not served from the same origin.
